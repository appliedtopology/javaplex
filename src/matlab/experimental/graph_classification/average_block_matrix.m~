function matrix = average_block_matrix(block_matrix, I, J)
    m = size(block_matrix, 1);
    n = size(block_matrix, 2);
    block_size_1 = m / I;
    block_size_2 = n / I;
    
    matrix = zeros(I, J);
    
    for i = 1:I
        for j = 1:J
            
        end
    end
end